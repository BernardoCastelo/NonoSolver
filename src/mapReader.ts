import puppeteer, { HTTPResponse, Page } from 'puppeteer';
import { of } from 'rxjs';

export async function getMap(page: HTTPResponse): Promise<string[][]> {
    var map = [[""]];


    return map;
}